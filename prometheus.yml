global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: emr-backend
    metrics_path: /metrics
    static_configs:
      # backend runs as a container on the compose network; reach it by service name.
      - targets: ["backend:8080"]
        labels:
          service: emr-backend